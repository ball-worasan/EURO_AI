# ============================================
# MetaTrader5 (MT5) Configuration (ไม่ใช้ก็ปล่อยว่างได้)
# ============================================

MT5_LOGIN=
MT5_PASSWORD=
MT5_SERVER=

# ============================================
# Project Paths
# ============================================

EURO_AI_BASE_DIR=

# ============================================
# AI Model API (สำคัญ)
# ============================================
# URL ของ FastAPI ที่รันโมเดล AI อยู่
# ถ้าคุณรัน server ในเครื่องเดียวกัน (server.py / main.py) ใช้อันนี้ได้เลย

AI_BASE_URL=http://127.0.0.1:8000

# จำนวน warmup bars ที่ใช้คำนวณ indicator ก่อนเข้าโมเดล
# ในโค้ดเราใช้ default 200 แต่ตั้งใน .env ได้จะชัดกว่า

MODEL_WARMUP_BARS=200

# ============================================
# Server Configuration (Optional)
# ============================================
# SERVER_PORT=8000
# SERVER_HOST=0.0.0.0

# ============================================
# PyTorch / GPU Configuration (Optional)
# ============================================
# FORCE_CPU=false

# ============================================
# Logging (Optional)
# ============================================

LOG_LEVEL=INFO
